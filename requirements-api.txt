# FastAPI Backend Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
pydantic>=2.0
pyyaml>=6.0

# Core ClimSight dependencies (already in requirements.txt)
# langchain, langchain-openai, chromadb, openai, etc.
